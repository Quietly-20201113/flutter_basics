name: flutterbasics
description: 搭建一个基础的小框架,尝试搭建 provider与bloc混合

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  bdmap_location_flutter_plugin:
#    path: plugins/bdmap_location_flutter_plugin
  cupertino_icons: ^0.1.2
  sentry: ">=3.0.0 <4.0.0"
  flutter_spinkit: "^4.1.2"
  oktoast: ^2.3.2
  url_launcher : ^5.4.2
  flutter_bmfmap: ^1.0.1
  flutter_bmfutils: ^1.0.1
  provider: ^4.0.3
  flutter_markdown : ^0.3.4
  flutter_easyrefresh : ^2.1.1
  common_utils: ^1.2.1
  audioplayers: ^0.15.1
  expressions :  ^0.1.5
#  #百度地图定位
#  bdmap_location_flutter_plugin: ^0.0.1
  dio: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/markdown/navigation.md



